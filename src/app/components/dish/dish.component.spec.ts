/* tslint:disable:no-unused-variable */

import {DishComponent} from "./dish.component";

describe('Component: Dish', () => {
  it('should createProduct an instance', () => {
    let component = new DishComponent();
    expect(component).toBeTruthy();
  });
});
